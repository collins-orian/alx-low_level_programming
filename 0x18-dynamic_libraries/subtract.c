#include "main.h"

/**
 * sub - function to add two values
 *
 * Description: subtracts the two parameters passed to it
 * @x: parameter 1
 * @y: parameter 2
 * Return: the difference of the two values
 */

int sub(int x, int y)
{
        return (x - y);
}
