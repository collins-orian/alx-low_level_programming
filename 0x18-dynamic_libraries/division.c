#include "main.h"

/**
 * div - function to divide two values
 *
 * Description: divides the two parameters passed to it
 * @x: parameter 1
 * @y: parameter 2
 * Return: the division of the two values
 */

int divi(int x, int y)
{
        return (x / y);
}
