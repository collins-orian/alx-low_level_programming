#include "main.h"

/**
 * mod - remainder function
 *
 * Description: it divides two values and return their remainder
 * @x: parameter 1
 * @y: parameter 2
 * Return: the remainder of the division of the two values
 */

int mod(int x, int y)
{
        return (x % y);
}
