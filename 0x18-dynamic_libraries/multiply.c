#include "main.h"

/**
 * mul - function to multiply two values
 *
 * Description: multiplies the two parameters passed to it
 * @x: parameter 1
 * @y: parameter 2
 * Return: the multiplication of the two values
 */

int mul(int x, int y)
{
	return (x * y);
}
