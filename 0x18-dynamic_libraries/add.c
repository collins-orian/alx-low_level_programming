#include "main.h"

/**
 * add - function to add two values
 *
 * Description: adds the two parameters passed to it
 * @x: parameter 1
 * @y: parameter 2
 * Return: the sum of the two values
 */

int add(int x, int y)
{
	return (x + y);
}
